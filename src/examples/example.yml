description: >
  Sample example description.
usage:
  version: 2.1
  orbs:
    <orb-name>: jesiio/require-git-not-dirty@version
  jobs:
    your-job:
      steps:
        - require-git-not-dirty/test
